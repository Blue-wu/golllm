# RAG 知识库系统 - 代码阅读指南

## 📋 项目概览

本项目是一个基于 Go 语言实现的 RAG（检索增强生成）知识库问答系统，核心功能包括：
- **语义切片**：基于句子相似度的智能文本分段
- **父子文档召回**：子块检索 + 父块生成，保证回答完整性
- **混合召回**：向量检索（语义匹配）+ BM25关键词检索（精确匹配）
- **RRF融合排序**：合并两种检索结果，提升召回准确率

## 🏗️ 项目架构

```
golllm/
├── cmd/rag/              # 主应用入口
│   ├── main.go           # 启动入口
│   ├── config/           # 配置管理
│   ├── handler/          # HTTP 控制器
│   ├── service/          # 业务逻辑层（核心）
│   ├── repository/       # 数据访问层
│   └── model/            # 数据模型
└── pkg/                  # 公共包
    ├── text/             # 文本处理（语义切片、BM25、RRF）
    ├── embedding/        # 向量化客户端
    ├── llm/              # LLM 客户端
    └── vector/           # 向量存储客户端
```

## 📖 代码阅读顺序

### 第一阶段：理解核心概念（推荐先看）

| 顺序 | 文件路径 | 核心内容 | 阅读目的 |
|:---:|---------|---------|---------|
| 1 | `pkg/text/semantic_splitter.go` | 语义切片算法 | 理解如何基于句子相似度分段 |
| 2 | `pkg/text/bm25.go` | BM25关键词检索 | 理解关键词匹配算法原理 |
| 3 | `pkg/text/rrf.go` | RRF融合排序 | 理解如何合并两种检索结果 |

### 第二阶段：数据模型与存储

| 顺序 | 文件路径 | 核心内容 | 阅读目的 |
|:---:|---------|---------|---------|
| 4 | `cmd/rag/model/document.go` | 文档、切片、父块数据结构 | 理解父子文档的数据模型 |
| 5 | `cmd/rag/repository/sqlite.go` | SQLite 数据库操作 | 理解数据持久化逻辑 |

### 第三阶段：业务逻辑层

| 顺序 | 文件路径 | 核心内容 | 阅读目的 |
|:---:|---------|---------|---------|
| 6 | `cmd/rag/service/document.go` | 文档处理服务 | 理解文档上传→切片→向量化流程 |
| 7 | `cmd/rag/service/rag.go` | RAG 问答服务 | 理解混合召回→上下文管理→LLM生成流程 |

### 第四阶段：接口层

| 顺序 | 文件路径 | 核心内容 | 阅读目的 |
|:---:|---------|---------|---------|
| 8 | `cmd/rag/handler/document.go` | 文档相关 API | 理解文档上传、列表、删除接口 |
| 9 | `cmd/rag/handler/chat.go` | 聊天问答 API | 理解问答、历史记录接口 |
| 10 | `cmd/rag/main.go` | 应用启动入口 | 理解服务初始化和路由注册 |

## 🧠 核心功能详解

### 1. 语义切片

**文件**: `pkg/text/semantic_splitter.go`

**核心流程**:
```
原始文本 → 句子拆分 → 批量Embedding → 计算相邻句子相似度 → 断点分割 → 父子块生成
```

**关键函数**:
- `SplitBySemantic`: 主入口，执行完整语义切片流程
- `splitIntoSentences`: 按标点符号拆分句子
- `calculateSimilarity`: 计算两个向量的余弦相似度
- `findBreakPoints`: 根据相似度阈值找出分割点

**设计要点**:
- 使用标题作为自然分割点（优先级最高）
- 相似度低于阈值（默认0.7）时分割，避免语义断裂
- 子块长度控制在合理范围（50-200字符）

### 2. BM25关键词检索

**文件**: `pkg/text/bm25.go`

**核心流程**:
```
文档集合 → 分词 → 计算词频/逆文档频率 → 构建索引 → 查询打分 → 返回排序结果
```

**关键函数**:
- `BuildBM25Document`: 构建 BM25 文档（分词+词频统计）
- `BuildIndex`: 构建检索索引（计算平均文档长度）
- `Retrieve`: 执行关键词检索（计算 BM25 分数）
- `score`: 计算单个文档的 BM25 分数

**BM25 公式**:
```
score = Σ [ IDF(q_i) × TF(q_i,d) × (k1+1) / (TF(q_i,d) + k1 × (1-b+b×|d|/avgdl)) ]
```

**参数说明**:
- `k1`: 控制词频饱和程度（默认1.2）
- `b`: 控制文档长度归一化（默认0.75）

### 3. RRF混合召回

**文件**: `pkg/text/rrf.go`

**核心流程**:
```
向量检索结果 + BM25检索结果 → 去重 → RRF打分 → 排序 → 返回最终结果
```

**RRF 公式**:
```
score = Σ (1 / (k + rank))
```
其中 k 为常数（默认60），rank 为文档在各检索结果中的排名

**关键函数**:
- `MergeResults`: 合并向量检索和 BM25 检索结果
- `RRFScore`: 计算 RRF 分数

**设计要点**:
- 相同文档片段去重，保留最高排名
- 两种检索结果同等权重，互补提升召回率

### 4. 父子文档数据结构

**文件**: `cmd/rag/model/document.go`

**核心数据结构**:
```go
// ParentChunk: 父块，用于生成完整回答
type ParentChunk struct {
    ID       int64  // 唯一标识
    DocID    int64  // 所属文档ID
    Content  string // 完整段落内容
    Heading  string // 所属标题
}

// DocumentChunk: 子块，用于检索
type DocumentChunk struct {
    ID       int64  // 唯一标识
    DocID    int64  // 所属文档ID
    ParentID int64  // 关联的父块ID（新增）
    Content  string // 子块内容（较短）
    Heading  string // 所属标题
}
```

**设计原理**:
- **子块**: 短小精悍，包含关键信息，检索精度高
- **父块**: 包含完整上下文，生成回答更连贯

### 5. 文档处理流程

**文件**: `cmd/rag/service/document.go`

**核心流程**:
```
文档上传 → 读取内容 → 语义切片 → 生成父子块 → 子块向量化 → 入库存储
```

**关键函数**:
- `UploadDocument`: 上传文档入口
- `ProcessDocument`: 后台处理文档
- `saveParentChunks`: 保存父块到 SQLite
- `saveChildChunks`: 保存子块到 SQLite 和向量库

**流程图**:
```
┌─────────────────────────────────────────────────────────┐
│                    UploadDocument                       │
│                   (handler层调用)                        │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    ProcessDocument                       │
│                (goroutine异步处理)                        │
└─────────────────────────────────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        ▼                 ▼                 ▼
┌──────────────┐   ┌──────────────┐   ┌──────────────┐
│ 读取文件内容   │   │ 语义切片处理   │   │ 父子块生成   │
└──────────────┘   └──────────────┘   └──────────────┘
                                          │
                    ┌─────────────────────┼─────────────────────┐
                    ▼                     ▼                     ▼
            ┌──────────────┐      ┌──────────────┐      ┌──────────────┐
            │ 保存父块到    │      │ 子块批量      │      │ 保存子块到    │
            │ SQLite       │      │ Embedding    │      │ SQLite+向量库 │
            └──────────────┘      └──────────────┘      └──────────────┘
```

### 6. RAG 问答流程

**文件**: `cmd/rag/service/rag.go`

**核心流程**:
```
用户提问 → 混合召回（向量+BM25）→ RRF融合排序 → 映射到父块 → 构建Prompt → LLM生成回答
```

**关键函数**:
- `Ask`: 问答入口
- `HybridRecall`: 混合召回核心逻辑
- `VectorRecall`: 向量语义检索
- `BM25Recall`: BM25关键词检索
- `buildPromptWithContext`: 构建带上下文的 Prompt

**流程图**:
```
┌─────────────────────────────────────────────────────────┐
│                        Ask                              │
│                   (handler层调用)                        │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                   HybridRecall                          │
│              (向量检索 + BM25检索)                       │
└─────────────────────────────────────────────────────────┘
                          │
          ┌───────────────┴───────────────┐
          ▼                               ▼
┌──────────────────┐           ┌──────────────────┐
│   VectorRecall   │           │    BM25Recall    │
│  (语义相似度)     │           │   (关键词匹配)    │
└──────────────────┘           └──────────────────┘
          │                               │
          └───────────────┬───────────────┘
                          ▼
┌─────────────────────────────────────────────────────────┐
│                      RRF Merge                          │
│                   (去重 + 排序)                          │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              映射到父块 (GetParentByChildID)              │
│           (用子块ID查找完整父块内容)                       │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              buildPromptWithContext                     │
│      (拼接上下文 + 历史记录 + 检索内容 + 问题)            │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                        LLM Generate                      │
│                    (调用 Ollama 生成回答)                 │
└─────────────────────────────────────────────────────────┘
```

## 🔄 调用链完整图

```
用户请求
    │
    ▼ HTTP API
┌─────────────────────────┐
│  cmd/rag/handler/*.go   │  ← 控制器层：接收请求，返回响应
└─────────────────────────┘
    │
    ▼ Service 调用
┌─────────────────────────┐
│ cmd/rag/service/*.go    │  ← 业务逻辑层：核心处理逻辑
└─────────────────────────┘
    │
    ├───→ pkg/text/*.go   ← 文本处理：语义切片、BM25、RRF
    │
    ├───→ pkg/embedding   ← 向量化：调用 Ollama
    │
    ├───→ pkg/llm         ← LLM：调用 Ollama 生成
    │
    ├───→ pkg/vector      ← 向量存储：内存/Milvus
    │
    └───→ repository      ← 数据持久化：SQLite
```

## 📌 关键代码行标注

### 语义切片核心逻辑
`pkg/text/semantic_splitter.go:45-60` - 相似度计算与断点查找

### BM25 打分公式
`pkg/text/bm25.go:80-95` - BM25 分数计算

### RRF 融合算法
`pkg/text/rrf.go:20-45` - 合并两种检索结果

### 混合召回入口
`cmd/rag/service/rag.go:130-160` - HybridRecall 函数

### 父子块映射
`cmd/rag/service/rag.go:170-190` - 根据子块ID获取父块内容

### 文档处理入口
`cmd/rag/service/document.go:100-130` - ProcessDocument 函数

## 💡 阅读建议

1. **先理解数据流**：从 API 入口追踪到核心逻辑
2. **重点关注新增功能**：
   - `semantic_splitter.go`: 语义切片（新增）
   - `bm25.go`: BM25检索（新增）
   - `rrf.go`: RRF融合（新增）
   - `document.go` 中父子块处理（修改）
   - `rag.go` 中混合召回逻辑（修改）
3. **对照测试验证**：运行 `TEST_GUIDE.md` 中的测试用例，边看边验证
4. **关注配置文件**：`config/config.toml` 理解各组件配置方式

## 🚀 快速启动

```bash
# 进入项目目录
cd cmd/rag

# 编译运行
go run .

# 启动成功后访问
# http://localhost:8080/api/v1/health
```

## 📝 待优化项

1. **流式输出**：添加 SSE 接口实时返回生成内容
2. **Milvus 集成**：支持大规模数据存储
3. **更多文档格式**：PDF、Word 等
4. **Web UI**：添加前端界面
5. **增量更新**：支持文档修改后重新向量化