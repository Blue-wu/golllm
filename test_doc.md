# RAG 知识库系统

## 系统架构

RAG（Retrieval-Augmented Generation）系统由以下几个核心模块组成：

### 文档处理模块

文档处理模块负责接收用户上传的文档，进行文本解析和切片处理。支持多种文档格式，包括 Markdown、PDF、Word 等。文档首先经过解析提取纯文本内容，然后根据语义切片算法进行分段处理。

### 向量化模块

向量化模块使用 Embedding 模型将文本转换为向量表示。系统支持多种 Embedding 模型，包括 OpenAI 的 text-embedding-3-small、DeepSeek 的 deepseek-embedding，以及 Ollama 本地模型如 nomic-embed-text。

### 向量存储模块

向量存储模块负责存储和检索向量数据。系统支持 Milvus 和内存两种存储方式。内存存储适合开发测试环境，而 Milvus 适合生产环境的大规模数据存储。

### 检索模块

检索模块实现了混合召回策略，结合向量检索和关键词检索。向量检索使用余弦相似度匹配语义相似的文档片段，关键词检索使用 BM25 算法进行精确匹配。两种检索结果通过 RRF（Reciprocal Rank Fusion）算法进行融合排序。

## 核心功能

### 语义切片

语义切片是一种基于句子相似度的文本分段方法，避免传统固定长度切片导致的句子截断问题。系统会计算相邻句子的语义相似度，当相似度低于阈值时进行分割，同时保留标题作为自然分割点。

### 父子文档召回

父子文档召回策略将文档分为父块和子块。子块用于精确检索，父块包含完整的上下文信息用于生成回答。召回时先通过子块找到相关文档，再映射到父块获取完整内容，确保回答的完整性和连贯性。

### 混合召回

混合召回策略结合了向量检索和关键词检索的优点。向量检索擅长语义匹配，可以找到含义相似的文档；关键词检索擅长精确匹配，可以找到包含特定术语的文档。通过 RRF 算法融合两种结果，显著提升召回准确率。

## 技术栈

- **语言**: Go 1.22+
- **框架**: Gin
- **向量数据库**: Milvus / 内存存储
- **Embedding**: Ollama (nomic-embed-text)
- **LLM**: Ollama (Qwen2.5-7B)
- **数据库**: SQLite

## API 接口

### 文档上传

POST /api/v1/documents/upload

支持上传 Markdown、PDF、Word 等格式的文档。

### 文档列表

GET /api/v1/documents/list

获取文档列表，支持分页查询。

### 文档详情

GET /api/v1/documents/{id}

获取文档详细信息，包括切片内容。

### 文档删除

DELETE /api/v1/documents/{id}

删除文档及其相关的向量数据。

### 问答接口

POST /api/v1/chat/ask

提交问题，获取基于知识库的回答。

### 聊天历史

GET /api/v1/chat/history/{session_id}

获取指定会话的聊天历史。

## 配置说明

系统支持通过配置文件自定义各项参数，包括服务器地址、向量数据库类型、Embedding 模型、LLM 模型等。配置文件使用 TOML 格式，位于 config/config.toml。

## 部署方式

### 开发环境

```bash
cd cmd/rag
go run .
```

### 生产环境

```bash
go build -o rag ./cmd/rag
./rag
```

## 性能优化

### 向量索引

使用 HNSW 索引加速向量检索，支持自定义索引参数如 M、efConstruction 等。

### 批量处理

文档向量化支持批量处理，提高处理效率。

### 缓存策略

会话历史和检索结果可以缓存，减少重复计算。

## 安全考虑

- API 访问控制
- 文件上传类型验证
- 输入内容过滤
- 敏感信息保护

## 未来规划

- 支持更多文档格式
- 实现增量更新
- 添加用户认证
- 支持多语言
- 优化检索算法